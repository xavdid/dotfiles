#!/bin/zsh

# USAGE
# 1. Copy a URL
# 2. Run `imgsav filename.extension from post root
# 3. Paste into image markdown

if [ $# -eq 0 ]; then
    echo "supply a filename + extension as the first argument"
    exit 1
fi

curl --fail -s "$(pbpaste)" > "./$1";
printf "![](./$1)" | pbcopy;
