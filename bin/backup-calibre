#!/usr/bin/env zsh -ex

# create a backup of my whole calibre installation

EXPORT_LOCATION="~/Desktop/calibre-$(date -u +"%Y-%m-%d")"

calibre-debug --export-all-calibre-data $EXPORT_LOCATION all
mv $EXPORT_LOCATION "$DROPBOX/Archive/Calibre Backup"
